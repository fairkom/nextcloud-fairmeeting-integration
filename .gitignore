# Build artifacts
/node_modules
/vendor
/fairmeeting/vendor
/fairmeeting/node_modules
/translationtool.phar

# Release packages (the script writes here)
/releases/*
/signing_files/*
fairmeeting.zip
fairmeeting_v*.zip
fairmeeting_v*.tar.gz
fairmeeting-deploy.tar.gz

# macOS metadata
**/.DS_Store
._*

# Local-only dev/deploy scripts (host-specific paths, not for the public repo)
docker-compose.nextcloud.yml
start-local-instance-and-deploy.sh
deploy-production.sh
deploy-full.sh
deploy-to-dev.sh

# Agent / local notes
CLAUDE.md
